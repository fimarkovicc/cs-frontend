import styled from "styled-components"

export const CitiesStyled = styled.div`
    .cities-list-wrapper {
        display: flex;
        flex-wrap: wrap;

        div {
            flex-basis: 13%;
        }
    }
`